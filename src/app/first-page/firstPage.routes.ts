import { Routes } from '@angular/router';
import { FirstPageComponent } from './components/first-page-component/firstPage.component';

export const routes: Routes = [
  {
    path: '',
    component: FirstPageComponent,
    title: 'Hyper Company | One',
  },
];
